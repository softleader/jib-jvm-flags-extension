name: Commitlint
on:
  pull_request:
    branches: [main]

jobs:
  commitlint:
    runs-on: [self-hosted, ubuntu-jammy]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: wagoid/commitlint-github-action@v6
